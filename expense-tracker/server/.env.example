# Google Sheets API
# 1. Create a Google Cloud project, enable Google Sheets API
# 2. Create a Service Account, download JSON key
# 3. Create a Google Sheet, add header row: Date, Time, Amount, Category, Payment Mode, Notes, Created Timestamp
# 4. Share the Sheet with the service account email (Editor)

PORT=3001
SHEET_ID=your_google_sheet_id_here
# Local dev: path to JSON key file (relative to server/)
GOOGLE_APPLICATION_CREDENTIALS=./your-service-account-key.json
# Production (e.g. Render): paste the entire JSON key content as a single line
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# Auth (required for multi-user). Use a long random string (e.g. openssl rand -hex 32).
JWT_SECRET=your_jwt_secret_here

# Optional: restrict CORS (comma-separated origins; leave empty to allow all in dev)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
