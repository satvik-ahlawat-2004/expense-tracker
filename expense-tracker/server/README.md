# Expense Tracker – Backend

Run from **this folder** (`expense-tracker/server`):

```bash
npm install   # if you haven't already
npm run dev   # start with watch mode
```

From the **project root** (`expense trackter project`), use:

```bash
npm run dev:server
```

See the main [README](../README.md) for full setup (Google Sheet, .env, JWT_SECRET).
